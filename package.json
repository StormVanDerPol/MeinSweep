{
  "name": "mein-sweep",
  "version": "0.0.1",
  "description": "Jawohl",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^2.7.0",
    "gh-pages": "^4.0.0"
  },
  "scripts": {
    "example": "parcel serve ./example/index.html",
    "deploy": "NODE_ENV=production parcel build ./example/index.html && gh pages -d dist"
  }
}
